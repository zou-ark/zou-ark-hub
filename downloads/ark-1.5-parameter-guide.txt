| Stat FPS |  // 启用帧率显示
| r.Lumen.DiffuseIndirect.Allow 1 |  // 启用间接光照（洞内颜色丢失重新输入这个）
| t.MaxFPS 0 |  // 无帧数限制
| r.VolumetricCloud 0 |  // 禁用体积云
| r.VolumetricFog 0 |  // 禁用体积雾霾
| r.BloomQuality 0 |  // 禁用Bloom效果d
| r.LightShaftQuality 0 |  // 禁用光晕效果
| grass.sizeScale 0 |  // 禁用草地大小
| R.ShadowQuality 0 |  // 禁用阴影
| r.Lumen.Reflections.Allow 0 |  // 禁用反射效果
| r.Water.SingleLayer.Reflection 0 |  // 禁用水面反射
| wp.Runtime.HLOD.ForceDisable 1 |  // 禁用HLOD（大范围对象的LOD优化）
| Gamma 4 |  // 设置亮度（屏幕过亮或者过暗调这个）
| r.Nanite.MaxPixelsPerEdge 5 |  // 降低Nanite的细节
| r.MipMapLODBias 1 |  // 降低MipMap级别
| r.Tonemapper.Sharpen 2 |  // 禁用锐化
| grass.densityscale 0 |  // 禁用草地密度
| r.Shadow.Virtual.Enable 0 |  // 禁用虚拟阴影
| r.Shadow.CSM.MaxCascades 0 |  // 禁用阴影最大级联
| sg.FoliageQuality 0 |  // 禁用植被质量
| sg.TextureQuality 0 |  // 禁用纹理质量
| wp.Runtime.HLOD 0 |  // 禁用HLOD
| r.DistanceFieldShadowing 0 |  // 禁用距离场阴影
| r.Streaming.PoolSize 1 |  // 设置纹理流池最小值
| r.Lumen.ScreenProbeGather.RadianceCache.ProbeResolution 16 |  // 降低光照探针分辨率
| r.PostProcessing.DisableMaterials 1 |  // 禁用后处理材质
| r.ContactShadows 0 |  // 禁用接触阴影
| r.depthoffieldquality 0 |  // 禁用景深
| r.lightshafts 0 |  // 禁用光线散射
| r.LightCulling.Quality 0 |  // 禁用光照剔除
| foliage.LODDistanceScale 0 |  // 禁用植被LOD
| r.shadowquality 0 |  // 禁用阴影质量
| r.DynamicGlobalIlluminationMethod 1 |  // 禁用动态全局光照
| r.SkylightIntensityMultiplier 5 |  // 设置天空光强度为最低
| r.Shading.FurnaceTest.SampleCount 0 |  // 禁用焚炉测试采样
| r.ScreenPercentage 50 |  // 设置屏幕渲染比例（降低分辨率来提升性能）
| r.detailmode 0 |  // 禁用细节模式
| r.viewdistance 0 |  // 降低视距（怕撞别人炮塔上数值调高）
| r.foliage.WPODisableMultiplier 1 |  // 禁用WPO（植被优化）乘数
| r.foliage.AutoBoundsWPODisableMax 1 |  // 禁用自动WPO界限
| r.shadow.virtual.enable 0 |  // 禁用虚拟阴影
| r.contactshadows 0 |  // 禁用接触阴影
| r.streaming.poolsize 1 |  // 设置纹理流池最小值
| r.mipmaplodbias 0 |  // 禁用MipMap LOD偏置
| sg.texturequality 0 |  // 禁用纹理质量
| r.vsync 0 |  // 禁用垂直同步
| show InstancedFoliage |  // 显示实例化植被
| show InstancedGrass |  // 显示实例化草地
| show InstancedStaticMeshes |  // 显示实例化静态网格
| r.Nanite.MaxPixelsPerEdge 3 |  // 降低Nanite的细节
| r.BloomQuality 0 |  // 禁用Bloom效果
| r.Tonemapper.Sharpen 2 |  // 禁用锐化
| r.fog 0 |  // 禁用雾霾效果
| ark.MaxActiveDestroyedMeshGeoCollectionCount 0 |  // 限制最大销毁网格数
| sg.GlobalIlluminationQuality 0 |  // 禁用全局光照
| r.Shadow.Virtual.Enable 0 |  // 禁用虚拟阴影
| r.SkyAtmosphere 0 |  // 禁用天空大气
| r.AOOverwriteSceneColor 0 |  // 禁用AO场景色覆盖
| Slate.GlobalScrollAmount 50 |  // 设置全局滚动量
| r.VT.EnableFeedback 1 |  // 启用纹理反馈
| fx.MaxNiagaraGPUParticlesSpawnPerFrame 1 |  // 限制每帧的Niagara粒子数
| SLATE.CONTRAST -1 |  // 设置对比度（-1反色调，0透明，1原版色）数字越高ui饱和度越高反之则越低